É nessa pasta "config" que ficam os arquivos que se
existirem causam um efeito em cadeia na inicialização
aplicação.

Por exemplo o diretório para onde será importado e exportado
o xml é setado em um arquivo binário dessa pasta