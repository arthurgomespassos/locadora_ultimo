#ifndef LOCADORA_ULTIMO_M_INIT_H
#define LOCADORA_ULTIMO_M_INIT_H

// cria as pastas e arquivos necessários para o funcionamento do programa caso eles não existam
void init(void);

// deleta todos os arquivos binarios da aplicação, mas as pastas da aplicação permanecem do mesmo jeito
void deletaTodosOsArquivosBinarios(void);

#endif //LOCADORA_ULTIMO_M_INIT_H
